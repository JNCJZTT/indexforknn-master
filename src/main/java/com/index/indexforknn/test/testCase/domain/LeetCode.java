package com.index.indexforknn.test.testCase.domain;

/**
 * TODO
 * 2022/3/20 zhoutao
 */
public class LeetCode {
    // a b 数组   sum= i1->n(ai * bi) a [x,y] sum max  a 1 3 2 4  b 1 2 3 4
    public void sumArray(int a[], int b[]) {
        int n = a.length;
        int sum = a[0] * b[0];
        int diff = 0;
        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {

            }
        }
    }
}
